const Note = () => {
  return (
    <div className="mt-8 pt-4 border-t border-gray-200">
      <p className="text-xs text-gray-500">
        Note: Matches Tailwind CSS v3.0+ default color palette. Black and white
        are special cases without shades.
      </p>
    </div>
  );
};

export default Note;
